import { describe, expect, test } from "@jest/globals";
import { part1, part2 } from "./day{{dayNumber}}";

describe("Day {{dayNumber}}", () => {
  const testInput = [];

  describe("part1", () => {
    test("should solve for test input", () => {
      expect(part1(testInput)).toBe(0);
    });
  });

  describe("part2", () => {
    test("should solve for test input", () => {
      expect(part2(testInput)).toBe(0);
    });
  });
});
